---
// Example custom Image component
// No props interface is required for basic usage, but you can define one for more complex components
---

<!-- Props are directly accessible via Astro.props -->
<img src={Astro.props.src} alt={Astro.props.alt} class={Astro.props.class ?? ''} />
