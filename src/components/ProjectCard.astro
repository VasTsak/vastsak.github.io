---
// Import your custom or Astro-provided Image component
import Image from '../path/to/your/Image.astro';

// Your existing props and frontmatter logic
import type { ProjectEntry } from "types";

export interface Props {
  href?: string;
  frontmatter: ProjectEntry["data"];
}

const { href, frontmatter } = Astro.props;
const { title, description, projectImage } = frontmatter;
---

<li class="card">
  <Image src={projectImage} alt={title} class="image" loading="eager" />
  <div>
    <a class="link" href={href}>
      <h2 class="header line-clamp-2-inline">{title}</h2>
    </a>
    <p class="line-clamp-3-inline">{description}</p>
  </div>
</li>
